import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const rooty = ReactDOM.createRoot(document.getElementById("root"));

rooty.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
